# ============================================================
# Local development defaults (safe for local self-host / dev)
# Copy to .env to override.
#
# Railway deployment: see docs/railway-deployment.md
#   - Do NOT use this file for Railway; set variables in each Service's
#     Variables tab in the Railway Dashboard instead.
# ============================================================

# --- app ports (host, local compose only) ---
FRONTEND_PORT=3000
BACKEND_PORT=8000

# --- database (local compose only) ---
POSTGRES_DB=mission_control
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_PORT=5432

# --- Redis (local compose only; Railway injects REDIS_URL automatically) ---
REDIS_URL=redis://localhost:6379/0

# --- backend settings (see backend/.env.example for full list) ---
# Railway: set DATABASE_URL to postgresql+psycopg://... (note the +psycopg driver suffix)
DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/mission_control
CORS_ORIGINS=http://localhost:3000
DB_AUTO_MIGRATE=true
LOG_LEVEL=INFO
LOG_FORMAT=text
REQUEST_LOG_SLOW_MS=1000
AUTH_MODE=local
# REQUIRED when AUTH_MODE=local (must be non-placeholder and at least 50 chars).
# Generate with: openssl rand -hex 32
LOCAL_AUTH_TOKEN=

# --- frontend settings ---
# REQUIRED: Public URL used by the browser to reach the API.
# Local dev: http://localhost:8000
# Railway:   https://<your-backend>.up.railway.app  (max 64 chars)
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_AUTH_MODE=local
