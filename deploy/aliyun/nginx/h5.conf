# Reverse proxy for H5 Mobile Web App (Phase 5 — M7).
# Placeholder configuration; activate when the h5-app service is deployed.
# Sits behind Alibaba Cloud SLB which terminates HTTPS.

# Uncomment when h5-app is deployed:
# upstream h5_app {
#     server h5-app:3001;
# }
#
# server {
#     listen 80;
#     server_name h5.xxx.com;
#
#     # SLB terminates SSL; trust X-Forwarded-* headers from SLB.
#     set_real_ip_from 10.0.0.0/8;
#     set_real_ip_from 172.16.0.0/12;
#     real_ip_header X-Forwarded-For;
#
#     # Security headers
#     add_header X-Frame-Options DENY always;
#     add_header X-Content-Type-Options nosniff always;
#     add_header X-XSS-Protection "1; mode=block" always;
#     add_header Referrer-Policy strict-origin-when-cross-origin always;
#
#     client_max_body_size 2m;
#
#     location / {
#         proxy_pass http://h5_app;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_set_header X-Forwarded-Host $host;
#
#         proxy_connect_timeout 10s;
#         proxy_read_timeout 30s;
#         proxy_send_timeout 30s;
#     }
#
#     # Static assets caching
#     location /assets/ {
#         proxy_pass http://h5_app;
#         proxy_set_header Host $host;
#         expires 365d;
#         add_header Cache-Control "public, immutable";
#     }
# }
