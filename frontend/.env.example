# REQUIRED: base URL for frontend -> backend calls (must be set for Activity feed and other API calls).
# Must be reachable from the browser (host).
NEXT_PUBLIC_API_URL=http://localhost:8000

# Authentication mode (must match backend AUTH_MODE)
# Options: clerk, local_bearer, disabled
NEXT_PUBLIC_AUTH_MODE=disabled

# Clerk authentication (optional - alternative to local auth)
# Set these to use Clerk for authentication (production/SaaS deployments)
# Leave blank to disable Clerk (for local development or self-hosting)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
NEXT_PUBLIC_CLERK_SIGN_IN_FORCE_REDIRECT_URL=/boards
NEXT_PUBLIC_CLERK_SIGN_UP_FORCE_REDIRECT_URL=/boards
NEXT_PUBLIC_CLERK_SIGN_IN_FALLBACK_REDIRECT_URL=/boards
NEXT_PUBLIC_CLERK_SIGN_UP_FALLBACK_REDIRECT_URL=/boards
NEXT_PUBLIC_CLERK_AFTER_SIGN_OUT_URL=/
