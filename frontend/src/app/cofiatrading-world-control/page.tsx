import { WorldControl } from "@/components/cofiatrading-world-control/WorldControl";

export const metadata = {
  title: "COFIATRADING — New York World Control",
  description: "Mission Control unique — 100M ARR Castle, 15 maisons, agents, camions, preuves.",
};

export default function CofiatradingWorldControlPage() {
  return <WorldControl />;
}
