import { WorldMap } from "@/components/world-map";

export const metadata = {
  title: "CofiaTrading — World Map",
  description:
    "Centre de contrôle vivant : zones, maisons, agents, liens animés, console & télémétrie réseau.",
};

export default function WorldMapPage() {
  return <WorldMap />;
}
